# Compatibility entry point. New installations can register backstage/all.yaml directly.
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: platform-blueprints-entrypoint
  description: Compatibility entry point for the Platform Blueprints catalog.
spec:
  targets:
    - ./backstage/all.yaml
