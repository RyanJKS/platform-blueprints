# Azure databases

This directory reserves a place for database modules. No database modules are implemented yet.

Use service-specific child modules when adding support, such as `postgresql/`, `sql_database/`, `sql_managed_instance/`, and `cosmos_db/`. Each child module owns its inputs, outputs, documentation, and tests. This grouping directory contains no Terraform resources.
